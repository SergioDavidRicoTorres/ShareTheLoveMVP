import { Text, View } from "react-native";

export default function DebuggingScreen() {
  return (
    <View
      style={{
        flex: 1,
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <Text>Debugging Screen</Text>
    </View>
  );
}
